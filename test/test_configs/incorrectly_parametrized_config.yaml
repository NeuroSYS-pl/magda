modules:
  - name: ${NAME_2}
    type: ModuleDependingCorrect

  - name: mod1
    type: ${TYPE_1}

  - name: mod0
    type: ModuleSample
    depends_on:
      - mod1
      - mod2
    parameters:
      threshold : ${THRESHOLD}
      threshold2 : ${THRESHOLD}
      learning_rate : ${LEARNING_RATE}
