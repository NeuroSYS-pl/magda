modules:
  - name: ${NAME_2}
    type: ModuleSample

  - name: mod1
    type: ModuleSample

  - name: mod0
    type: ModuleSample
    depends_on:
      - mod1
      - mod2
    parameters:
      threshold : ${THRESHOLD}
      threshold2 : ${THRESHOLD}
      learning_rate : ${LEARNING_RATE}
